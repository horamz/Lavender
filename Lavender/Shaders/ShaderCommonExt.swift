extension VertexBufferBindPoints {
    var index: Int {
        Int(self.rawValue)
    }
}

extension TextureBindPoints {
    var index: Int {
        Int(self.rawValue)
    }
}

extension AttributeIndices {
    var index: Int {
        Int(self.rawValue)
    }
}
